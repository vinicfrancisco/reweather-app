import React from 'react';

import { Container, Header, HeaderTitle } from './styles';

const City: React.FC = () => {
  return (
    <Container>
      <Header>
        <HeaderTitle>City</HeaderTitle>
      </Header>
    </Container>
  );
};

export default City;
